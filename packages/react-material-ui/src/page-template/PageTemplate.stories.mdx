import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { PageTemplate } from './PageTemplate';
import { storyRootPath } from '../index.stories';

<Meta
  title={`${storyRootPath}/page-templates/Introduction`}
  component={PageTemplate}
/>

# Page Templates

Page Templates are full page layouts you can use to standardize screens across the app. These page templates are used to unify the user experience.

<Story id='react-material-ui-page-templates-pagetemplate--default' />
