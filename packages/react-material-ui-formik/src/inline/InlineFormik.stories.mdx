import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { InlineFormik } from './InlineFormik';
import { storyRootPath } from '../index.stories';

<Meta title={`${storyRootPath}/Inline/Introduction`} component={InlineFormik} />

# Inline Forms using Formik

The `InlineFormik` component and related form fields allow you to build interactive forms that save in real-time.

The following is an example of the inline forms in action:

<Story id='react-material-ui-formik-inline-inlineformik--default' />
